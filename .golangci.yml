run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - govet
    - gofmt
    - staticcheck
    - ineffassign
    - unconvert

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
